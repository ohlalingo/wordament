import { Request, Response, NextFunction } from "express";
import { db } from "../db.js";

// Admin guard: prefer AdminJS session, then shared admin token, then user-id check.
export async function adminAuth(req: Request, res: Response, next: NextFunction) {
  const path = req.path || "";
  const originalUrl = req.originalUrl || "";

  // 1) If AdminJS session is present, always allow
  const sessionUser = (req as any)?.session?.adminUser;
  if (sessionUser) return next();

  // 2) Allow AdminJS internal routes
  if (
    originalUrl.startsWith("/admin") ||
    path.startsWith("/resources") ||
    path.startsWith("/dashboard") ||
    path.startsWith("/pages") ||
    path.startsWith("/api")
  ) {
    return next();
  }

  // 3) Allow shared admin token
  const token = req.header("x-admin-token");
  if (token && process.env.ADMIN_TOKEN && token === process.env.ADMIN_TOKEN) {
    return next();
  }

  // 4) Fallback: x-user-id + email check
  const userId = Number(req.header("x-user-id"));
  if (!userId) {
    return res.status(401).json({ error: "Missing x-user-id or x-admin-token header" });
  }

  try {
    const { rows } = await db.query("SELECT email FROM users WHERE id = $1", [
